// given arr1 and arr2, 
// arr2's elements are distinct
// all elements in arr2 are also in arr1

// Sort arr1 such that the relative ordering of arr1 is the same as arr2.
// Eleemnts that aren't in arr2 should be at the end of arr1 in ascending order



const bob = 5;
console.log (bob + 5);